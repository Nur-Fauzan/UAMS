@{
    // View
    View = this;

    // Check action result
    if (refreshTokensDelete.ActionResult is EmptyResult) // Terminate page
        return;

    // Layout
    Layout = refreshTokensDelete.UseLayout ? "_Layout" : "";
}
<script>
var currentTable = @Html.Raw(ConvertToJson(refreshTokensDelete.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { RefreshTokens: currentTable } });
var currentPageID = ew.PAGE_ID = "delete";
var currentForm;
var fRefreshTokensdelete;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fRefreshTokensdelete")
        .setPageId("delete")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
@refreshTokensDelete.ShowPageHeader()
@refreshTokensDelete.ShowMessages()
<form name="fRefreshTokensdelete" id="fRefreshTokensdelete" class="ew-form ew-delete-form" action="@AppPath(CurrentPageName())" method="post" novalidate autocomplete="off">
@if (CurrentPage?.CheckToken ?? false) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="delete">
@foreach (string key in refreshTokensDelete.RecordKeys) {
<input type="hidden" name="key_m[]" value="@key">
}
<div class="card ew-card ew-grid @(refreshTokensDelete.TableGridClass)">
<div class="card-body ew-grid-middle-panel @(refreshTokensDelete.TableContainerClass)" style="@(refreshTokensDelete.TableContainerStyle)">
<table class="@(refreshTokensDelete.TableClass)">
    <thead>
    <tr class="ew-table-header">
@if (refreshTokensDelete.Id.Visible) { // refreshTokensDelete.Id.Visible (begin)
        <th class="@Html.Raw(refreshTokensDelete.Id.HeaderCellClass)"><span id="elh_RefreshTokens_Id" class="RefreshTokens_Id">@Html.Raw(refreshTokensDelete.Id.Caption)</span></th>
} @* refreshTokensDelete.Id.Visible (end) *@
@if (refreshTokensDelete.UserId.Visible) { // refreshTokensDelete.UserId.Visible (begin)
        <th class="@Html.Raw(refreshTokensDelete.UserId.HeaderCellClass)"><span id="elh_RefreshTokens_UserId" class="RefreshTokens_UserId">@Html.Raw(refreshTokensDelete.UserId.Caption)</span></th>
} @* refreshTokensDelete.UserId.Visible (end) *@
@if (refreshTokensDelete._Token.Visible) { // refreshTokensDelete._Token.Visible (begin)
        <th class="@Html.Raw(refreshTokensDelete._Token.HeaderCellClass)"><span id="elh_RefreshTokens__Token" class="RefreshTokens__Token">@Html.Raw(refreshTokensDelete._Token.Caption)</span></th>
} @* refreshTokensDelete._Token.Visible (end) *@
@if (refreshTokensDelete.ExpiryDate.Visible) { // refreshTokensDelete.ExpiryDate.Visible (begin)
        <th class="@Html.Raw(refreshTokensDelete.ExpiryDate.HeaderCellClass)"><span id="elh_RefreshTokens_ExpiryDate" class="RefreshTokens_ExpiryDate">@Html.Raw(refreshTokensDelete.ExpiryDate.Caption)</span></th>
} @* refreshTokensDelete.ExpiryDate.Visible (end) *@
    </tr>
    </thead>
    <tbody>
@{ refreshTokensDelete.RecordCount = 0; }
@while (refreshTokensDelete.Recordset != null && await refreshTokensDelete.Recordset.ReadAsync()) { // DN
    refreshTokensDelete.RecordCount++;
    refreshTokensDelete.RowCount++;

    // Set row properties
    refreshTokensDelete.ResetAttributes();
    refreshTokensDelete.RowType = RowType.View; // View

    // Get the field contents
    await refreshTokensDelete.LoadRowValues(refreshTokensDelete.Recordset);

    // Render row
    await refreshTokensDelete.RenderRow();
    <tr @Html.Raw(refreshTokensDelete.RowAttributes)>
@if (refreshTokensDelete.Id.Visible) { // refreshTokensDelete.Id.Visible (begin)
        <td @Html.Raw(refreshTokensDelete.Id.CellAttributes)>
<span id="">
<span @Html.Raw(refreshTokensDelete.Id.ViewAttributes)>
@Html.Raw(refreshTokensDelete.Id.GetViewValue())</span>
</span></td>
} @* refreshTokensDelete.Id.Visible (end) *@
@if (refreshTokensDelete.UserId.Visible) { // refreshTokensDelete.UserId.Visible (begin)
        <td @Html.Raw(refreshTokensDelete.UserId.CellAttributes)>
<span id="">
<span @Html.Raw(refreshTokensDelete.UserId.ViewAttributes)>
@Html.Raw(refreshTokensDelete.UserId.GetViewValue())</span>
</span></td>
} @* refreshTokensDelete.UserId.Visible (end) *@
@if (refreshTokensDelete._Token.Visible) { // refreshTokensDelete._Token.Visible (begin)
        <td @Html.Raw(refreshTokensDelete._Token.CellAttributes)>
<span id="">
<span @Html.Raw(refreshTokensDelete._Token.ViewAttributes)>
@Html.Raw(refreshTokensDelete._Token.GetViewValue())</span>
</span></td>
} @* refreshTokensDelete._Token.Visible (end) *@
@if (refreshTokensDelete.ExpiryDate.Visible) { // refreshTokensDelete.ExpiryDate.Visible (begin)
        <td @Html.Raw(refreshTokensDelete.ExpiryDate.CellAttributes)>
<span id="">
<span @Html.Raw(refreshTokensDelete.ExpiryDate.ViewAttributes)>
@Html.Raw(refreshTokensDelete.ExpiryDate.GetViewValue())</span>
</span></td>
} @* refreshTokensDelete.ExpiryDate.Visible (end) *@
    </tr>
}
@{ refreshTokensDelete.CloseRecordset(); }
</tbody>
</table>
</div>
</div>
<div class="ew-buttons ew-desktop-buttons">
<button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit">@Html.Raw(Language.Phrase("DeleteBtn"))</button>
<button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-href="@AppPath(refreshTokensDelete.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
</div>
</form>
@refreshTokensDelete.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
